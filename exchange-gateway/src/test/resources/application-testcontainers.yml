spring:
  main:
    allow-bean-definition-overriding: true

eureka:
  client:
    enabled: false

logging:
  level:
    root: WARN
    com.fintech.gateway: DEBUG

currency:
  service:
    base-url: http://localhost:8081